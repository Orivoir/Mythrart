model EbookType {
  id          String  @id @default(cuid())

  name        String
  slug        String  @unique
  description String?

  ebooks      Ebook[]

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}