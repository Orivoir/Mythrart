# Config files is used as Proxy old arch

this folder is used as proxy of `packages/*`
for backward compatibility with old arch

[see old arch at archive (private repo)](https://github.com/orivoir/mythart)
